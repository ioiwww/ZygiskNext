name: CI

on:
  [workflow_dispatch,push]
  

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      CCACHE_COMPILERCHECK: "%compiler% -dumpmachine; %compiler% -dumpversion"
      CCACHE_NOHASHDIR: "true"
      CCACHE_HARDLINK: "true"
      CCACHE_BASEDIR: "${{ github.workspace }}"

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: "recursive"
          fetch-depth: 0

      - name: Write key
        if: ${{ ( github.event_name != 'pull_request' && github.ref == 'refs/heads/master' ) || github.ref_type == 'tag' }}
        run: |
          mkdir -p module
          rm -f module/public_key module/private_key
          
          if [ ! -z "${{ secrets.PRIVATE_KEY }}" ]; then
            echo "Using keys from secrets..."
            echo '${{ secrets.PUBLIC_KEY }}' | base64 --decode > module/public_key
            echo '${{ secrets.PRIVATE_KEY }}' | base64 --decode > module/private_key
          else
            echo "Generating 32-byte keys using Linux urandom..."
            # توليد 32 بايت عشوائية تماماً للمفاتيح بدون تعقيدات البايثون
            head -c 32 /dev/urandom > module/private_key
            head -c 32 /dev/urandom > module/public_key
            echo "Keys generated successfully."
            echo "Private key size: $(stat -c%s module/private_key) bytes"
          fi

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: "17"

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Setup rust-cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: zygiskd/src -> ../build/intermediates/rust
          cache-targets: false

      - name: Setup Rust
        run: |
          rustup override set nightly
          rustup target add aarch64-linux-android x86_64-linux-android i686-linux-android armv7-linux-androideabi

      - name: Set up ccache
        uses: hendrikmuhs/ccache-action@v1.2
        with:
          max-size: 2G
          key: ${{ runner.os }}
          restore-keys: ${{ runner.os }}
          save: ${{ github.event_name == 'push' && github.ref == 'refs/heads/master' }}

      - name: Build with Gradle
        run: |
          chmod +x gradlew
          echo 'org.gradle.parallel=true' >> gradle.properties
          echo 'org.gradle.vfs.watch=true' >> gradle.properties
          echo 'org.gradle.jvmargs=-Xmx2048m' >> gradle.properties
          echo 'android.native.buildOutput=verbose' >> gradle.properties
          sed -i 's/org.gradle.unsafe.configuration-cache=true//g' gradle.properties
          ./gradlew :module:zipRelease
          ./gradlew :module:zipDebug

      - name: Prepare artifact
        if: success()
        id: prepareArtifact
        run: |
          releasePath=$(ls module/build/outputs/release/Zygisk-Next-v*-release.zip | head -n 1)
          debugPath=$(ls module/build/outputs/release/Zygisk-Next-v*-debug.zip | head -n 1)
          
          releaseName=$(basename "$releasePath" .zip)
          debugName=$(basename "$debugPath" .zip)
          
          echo "releaseName=$releaseName" >> $GITHUB_OUTPUT
          echo "debugName=$debugName" >> $GITHUB_OUTPUT
          
          unzip "$releasePath" -d zksu-release
          unzip "$debugPath" -d zksu-debug
          
          zip -r "SYMBOLS-$releaseName.zip" zygiskd/build/symbols/release
          zip -r "SYMBOLS-$debugName.zip" zygiskd/build/symbols/debug

      - name: Upload release
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.prepareArtifact.outputs.releaseName }}
          path: "./zksu-release/*"

      - name: Upload debug
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.prepareArtifact.outputs.debugName }}
          path: "./zksu-debug/*"

      - name: Upload release symbols
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.prepareArtifact.outputs.releaseName }}-symbols
          path: "zygiskd/build/symbols/release"

      - name: Upload debug symbols
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.prepareArtifact.outputs.debugName }}-symbols
          path: "zygiskd/build/symbols/debug"
